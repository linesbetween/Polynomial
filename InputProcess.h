/*CIT237 Polynomial
/*Hanfei XU
*/

/*Breaks down input string into vector of Terms
* Method:
* Locate the '+' or '-' charactor, then look after it. 
* If the input is correct, the order of each term should be -/+, letter, '^'(optional) and exp number(optional),
* and then another  -/+.
* Ignore all blanks.
*/

#include "Term.h"
#include <iostream>
#include <vector>
using namespace std;

vector <Term> inputProcess(string){

	vector<Term> termVec;
	//TODO

	return termVec;
}

